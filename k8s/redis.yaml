apiVersion: apps/v1
kind: Deployment
metadata:
  name: redis
  namespace: etf-dashboard
spec:
  replicas: 1
  selector:
    matchLabels:
      app: redis
  template:
    metadata:
      labels:
        app: redis
    spec:
      containers:
        - name: redis
          image: redis:7-alpine
          ports:
            - containerPort: 6379
          resources:
            requests:
              memory: "64Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "250m"
---
apiVersion: v1
kind: Service
metadata:
  name: redis-svc
  namespace: etf-dashboard
spec:
  selector:
    app: redis
  ports:
    - port: 6379
      targetPort: 6379
